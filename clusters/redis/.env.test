REDIS_PASSWORD=password
REDIS_USERNAME=username
REDIS_DATABASE=0
REDIS_KEY_PREFIX=redis_key_prefix
# Optional: limit memory per Redis instance (e.g. 256mb, 512mb, 1gb). Omit or leave empty for no limit.
REDIS_MAXMEMORY=256mb